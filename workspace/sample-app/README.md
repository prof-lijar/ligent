# Ligent Sample Workspace

This small project is the default demo target for Ligent. Use it to verify that the desktop app can send a software goal to the backend, persist the run, show agent outputs, and summarize the final state.

Suggested demo goal:

```text
Review the sample workspace app and plan a small change that adds a visible health status, implementation notes, QA checks, and documentation.
```
